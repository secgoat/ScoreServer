@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "Index", "LogOff") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "Index", "Logon") ]
}
